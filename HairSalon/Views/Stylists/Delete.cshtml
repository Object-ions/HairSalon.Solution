@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist
<div class="container">
<h2>Are you sure you want to delete this stylist profile?</h2>
<br />
@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)

@using (Html.BeginForm())
{
  <br/>
  <input type="submit" value="Delete" />
}

@Html.ActionLink("Back to stylists list", "Details", new { id = Model.StylistId})
</div>